```sh
mkdir -p ~/ipxe
cd ~/ipxe
wget http://boot.ipxe.org/Shell.efi
wget http://boot.ipxe.org/ipxe.efi
wget http://boot.ipxe.org/ipxe.pxe
wget http://boot.ipxe.org/snponly.efi
wget http://boot.ipxe.org/undionly.kpxe
wget http://boot.ipxe.org/tinycore.ipxe
wget http://boot.ipxe.org/texture.png
```

```sh
mkdir -p /srv/tftp/boot/iPXE/debian-installer
cd /srv/tftp/boot/iPXE/debian-installer
wget http://ftp.debian.org/debian/dists/bookworm/main/installer-amd64/current/images/netboot/netboot.tar.gz
tar -xvf ./netboot.tar.gz
rm netboot.tar.gz
```

```sh
nano /srv/tftp/boot/iPXE/boot.ipxe.cfg
#!ipxe

# Figure out if client is 64-bit capable
cpuid --ext 29 && set arch x64 || set arch x86
cpuid --ext 29 && set archl amd64 || set archl i386

###################### MAIN MENU ####################################

:start
menu iPXE boot menu
item --gap --             -------------------------------Installation ------------------------------
item --key l linux        Linux
item --gap --             ------------------------- Advanced options -------------------------------
item --key c config       Configure settings
item shell                Drop to iPXE shell
item reboot               Reboot computer
item
item --key x exit         Exit iPXE and continue BIOS boot
choose --timeout 5000 --default linux selected || goto cancel
set menu-timeout 0
goto ${selected}

:cancel
echo You cancelled the menu, dropping you to a shell

:shell
echo Type 'exit' to get the back to the menu
shell
set menu-timeout 0
set submenu-timeout 0
goto start

:failed
echo Booting failed, dropping to shell
goto shell

:reboot
reboot

:exit
exit

:config
config
goto start

:back
set submenu-timeout 0
clear submenu-default
goto start

############ MAIN MENU ITEMS ############

:linux
kernel boot/iPXE/debian-installer/linux initrd=boot/iPXE/debian-installer/initrd.gz auto=true url=tftp://192.168.1.21/preseed/file.cfg
initrd boot/iPXE/debian-installer/initrd.gz
boot
goto start
```